# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Anthropic (Claude API)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Finnhub (noticias empresa + mercado + earnings + fundamentals)
# Registro gratis: https://finnhub.io/register
FINNHUB_API_KEY=your_finnhub_api_key

# Cron job security (genera un string random seguro)
# Usado en vercel.json cron → GET /api/agent/run
CRON_SECRET=your_random_secret_here

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Usado en callbacks de Supabase (emails de confirmación y recuperación)
# En producción cambiar a la URL real del sitio
